{
  "devDependencies": {
    "concurrently": "^5.3.0",
    "json-server": "^0.16.1",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "axios": "^0.20.0"
  },
  "scripts": {
    "tscinit": "tsc --init",
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.html",
    "start": "concurrently npm:start:*"
  }
}
